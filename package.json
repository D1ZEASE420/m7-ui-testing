{
  "name": "m7-ui-testing",
  "version": "1.0.0",
  "type": "module",
  "description": "A teaching application for a Playwright UI testing course.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
