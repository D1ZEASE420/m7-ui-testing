import { test, expect } from "@playwright/test";
import { studentTest, BOOKS } from "./helpers.js";

studentTest.fixme("details page shows correct book title", async ({ page }) => {
  // TODO
});

studentTest.fixme("details page shows correct author", async ({ page }) => {
  // TODO
});

studentTest.fixme("details page shows category", async ({ page }) => {
  // TODO
});

studentTest.fixme("details page shows availability status", async ({ page }) => {
  // TODO
});

studentTest.fixme("back button returns to books page", async ({ page }) => {
  // TODO
});

studentTest.fixme("reserve action works from details page", async ({ page }) => {
  // TODO
});

studentTest.fixme("reserve button becomes disabled after reserving", async ({ page }) => {
  // TODO
});

studentTest.fixme("unavailable book has disabled reserve button on details page", async ({ page }) => {
  // TODO
});

studentTest.fixme("invalid book ID shows error message", async ({ page }) => {
  // TODO
});

test.fixme("unauthenticated user cannot access book details page", async ({ page }) => {
  // TODO
});
